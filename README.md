
TO INSTALL THE GAME

USE COMMANDS:

1) py -m venv venv      #CREATE PYTHON VENV
2) venv/Scripts/activate    #WORKING ON VENV
3) pip install -r requirements.txt   #INSTALLING PYTHON PACKAGES
4) ./build.ps1 || ./build.bat    #USE POWERSHELL OR CMD COMMANDS TO LOAD EXE 

